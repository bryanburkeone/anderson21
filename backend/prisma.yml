#endpoint: ${env:PRISMA_ENDPOINT}
endpoint: https://sick-fitzz-production.herokuapp.com/sick-fits-backend-service-prod/prod
#endpoint: https://sick-fitzz-production.herokuapp.com/sick-fits-prod/prod
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}
hooks:
  post-deploy:
    - graphql get-schema -p prisma
